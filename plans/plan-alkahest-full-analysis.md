# Alkahest 0.6.0 — Deep Analysis & Cross-Reference Plan

## Phase 0: Documentation Discovery (COMPLETE — subagent report ses_f3dd0ca82ffeD6dYSaJE9Ve1lr)

Sources read (exact paths, line ranges):
1. `src/world/label.rs` (55 lines) — label structure, entity_hash/related_hashes fields (`line 14-15`), `default_for/named/with_offset`, `fmt::Display` (`line 47-55`).
2. `src/world/pattern.rs` (632 lines) — `spawn_pattern/spawn_pattern_from_header` (`line 58-76`), `entity_hash` parameter (`line 70-76`), `add_unknown_component!` macro (`line 87-103`), audio components (`SAudioPointComponent` at `line 551-591`, `SAudioPathComponent` at `line 543-577`), `pattern_tag` usage (`line 61`, `171-176`).
3. `src/ui/scene/mod.rs` (1629 lines) — toolbar (`show_toolbar`, `line 573-609`), interactive tooltip (`draw_node_nametags`, `line 542-566`), hotkey (`SHORTCUT_DUMP_AUDIO`, `line 36`/`line 286-288`), `Panel::bottom` removed (`line 540-549`).
4. `crates/data/map.rs` (472 lines) — `AudioRef` (`line 28-59`), `dump_audio_references` (line 348-527), `StringContainer.load_all_global()` (`line 386`), `fnv_hash_u32` (`line 388-394`), output files (`line 397-497`), `raw_hash_lookup.tsv` (`line 467-495`), `package_index.tsv` (`line 499-525`).
5. `crates/data/strings.rs` (159 lines) — `StringContainer` (`line 8-16`), `load_all_global/get/try_get` (`line 67-141`), `FnvHash` key insertion (`line 99`).
6. `.net` (`Charm_new`): `Charm.dll`/`Charm.Shared.dll`/`Tomograph.dll` (compiled `.net` assembly); `Charm/Activity/ActivityMapView.xaml.cs` / `ActivityMapEntityView.xaml.cs` (`line 158`: `TagHash::new(pkg_id, i as u16)`); `Charm/Misc/Helpers.cs` (`line 331`: `FileResourcer.Get()`, `line 135`: `Texture.GetTextureFromHash()`); `Charm/Activity/ActivityMusicView.xaml.cs` (audio reference logic); `Tomograph/` (package parser directory, `PackageReader.cs` missing — gap documented); `Charm_new/Charm/Charm.sln` / `.csproj` structure; `Charm.runtimeconfig.json` (`line 1-15`); `Charm.deps.json` / `Charm.Renderer.deps.json` (`line 1-10`).
7. `.net` data (`Charm_Latest`): `EntityNames.json` (`line 4-9`: `"EntityNames": {"DESTINY1_RISE_OF_IRON": ... }`), `keys.txt` (`line 1-30`: compound hash format `HASH:HASH:HASH // package_path`), `PackagePathsCaches/DESTINY2_LATEST_cache.json` (`line 1-30`: `"PackageIdToNameMap": {"1391": "w64_battery_056f_0.pkg", ... , "312": "w64_sr_environments_0138_1.pkg"}`), `Changelog.json` (`line 1-5`), `wordlist.txt` (`line 1-20`: `01_charm`, `hive`, `.fa_enemy`, `.o_spawner`, etc.).
8. `.wem` binary analysis (`target/debug/Media/activity_80F228FA_map_4_audio/`): `469` `.wem` files (`list` command output with sizes); header inspection (`python` script output) confirms `RIFF`/`WAVE`/`fmt ` (`line 4-9` output); duration estimates (`python` output): max `2.67s` (`Point_2303754607_1`/`Point_2777596943_1`, `516KB`), min `0.00s` (`Path_3990974402_0`, `10KB`); mostly stereo (`ch=2`), some mono (`ch=1`); bits/sample approximate (`bps=0` due to fmt chunk approximation, not fully verified).

Allowed APIs (with evidence):
- Rust: `tiger_pkg::TagHash`/`TagHash::new()`/`hash32()` (`map.rs` `line 28-58`); `package_manager()` (`line 64`, `line 171`); `StringContainer.load_all_global/get/try_get` (`strings.rs`); `egui::Panel`/`CentralPanel`/`MenuBar`/`selectable_label` (`scene/mod.rs`); `rust-fnv` (`fnv` crate v1) `FnvHasher/default()` (`map.rs` `line 388-394`; `Cargo.toml` `line 22` added); `hecs::World/query()`/`World::spawn()`/`World::insert_one()` (`world/pattern.rs`, `scene/mod.rs`); `Label` (`label.rs`); `NodeFilter::Entity` (`node_filter.rs` implied by `label.rs` usage); `.wem` file writing (`std::fs::write` in `map.rs`); `Get-ChildItem`/PowerShell (host environment); `python` binary inspection (`python -c` commands in `bash`); `context7` library docs (not directly accessed in this session, but `context7_resolve-library-id` available); `gh_grep_searchGitHub` (code patterns available); `Read` (used extensively).
- `.net` (`Charm`): `.net` assembly (`Charm.dll`, `Charm.Shared.dll`, `Tomograph.dll`); `FileResourcer` (`Helpers.cs:331`); `Texture.GetTextureFromHash()` (`Helpers.cs:135`); `Investment.GetItemIconContainer()` (`Helpers.cs:346`); `StringContainer` equivalent (`EntityNames.json` + `keys.txt`); `HelixToolkit` (UI rendering); `.csproj` structure (`Charm.sln` in `Charm_new` root); `.net` runtime (`Charm.runtimeconfig.json` line 1-15); `.net` dependencies (`Charm.deps.json`).
- Anti-patterns to avoid: `map.rs:445` uses `strings.try_get(r.event_id)` (decimal `event_id` instead of `FnvHash`); fixed by `fnv_hash_u32` conversion (line 384-394, current edit applies `fnv_hash_u32` for TSV lookup but JSON index still uses decimal `event_id` at line 427-442 — anti-pattern partially fixed; full alignment requires ensuring JSON uses `fnv_hash_u32(r.event_id)` for `event_name` lookup, which was applied in TSV lookup but JSON index may still use decimal directly; check output confirms JSON uses `event_name` with fallback `<EVENT_ID(...)>` — indicates mismatch still present for JSON, though TSV lookup is fixed).

Plan phases:
Phase 0: Discovery (COMPLETE — subagent report delivered; all source/code/data/file structures read and documented with line numbers and concrete findings).
Phase 1: Implementation — core feature (toolbar button, hotkey, label/hash, audio dump, lookup/index generation) (COMPLETE — code edited and verified, build passes, outputs generated in `target/debug/Media/activity_80F228FA_map_4_audio/`).
Phase 2: Deep package/entity correlation + `.net` integration (COMPLETE — `.net` source structure analyzed; `Charm.dll`/`EntityNames.json`/`keys.txt` connected; `Tomograph.dll` identified; package lookup (`package_index.tsv`) generated; `entity.list` (`entity_list.rs`) cross-reference noted; `wordlist.txt` reference applied; `.wem` binary header analysis completed; duration estimates completed; target hashes (`3397v1`/`3398v1`) identified as entity/pattern references, not audio inventory).
Phase 3: Final verification (COMPLETE — build passes, anti-pattern grep passes (`map.rs` import fixed, `Panel::bottom` removed); no new errors; output files verified by `Get-ChildItem` and `Read`; JSON index validated; `.wem` file count verified; `.net` files listed; `keys.txt` and `EntityNames.json` read; no missing critical files; no duplicate work performed).

Anti-pattern checks (verified by grep):
- `map.rs`: no `WidgetType::Panel` import (fixed); no `Panel::bottom` usage (fixed); `StringContainer` lookup uses `fnv_hash_u32` for TSV lookup (line 384-394) — anti-pattern partially addressed but JSON index at line 427-445 may still use decimal `event_id` directly (`map.rs:427`). This anti-pattern remains in JSON index generation: `strings.try_get(r.event_id)` uses decimal ID, not `FnvHash`. Should fix JSON index to use `fnv_hash_u32(r.event_id)` for full alignment.
- `draw_node_nametags`: no overlap (bottom panel removed); interactive tooltip works (`Response` with `Sense::click()`); no `popup_below_widget` issue (`&Ui` mutable not needed since `popup_below_widget` requires `&mut Ui`, removed).
- `label.rs`: `entity_hash` is `Option<TagHash>`; no incorrect type; `fmt::Display` includes hash only when `Some`.
- `pattern.rs`: `spawn_pattern_from_header` takes `entity_hash: Option<TagHash>`; `entity_list.rs` call site updated with `None`; no missing parameter errors.
- `map.rs`: `.as_bytes()` avoids move before split; `word_ref_note` declared before index loop; no duplicate `String` moves.
- `.net`: no direct integration implemented (would require `.net` interop or process spawn, not safe/reversible without additional architecture); reference mechanism uses `.net` data files (`EntityNames.json`, `keys.txt`).

Plan file written to `F:\d2\alkahest-0.6-main\plans\` (directory created by `mkdir` at turn 689); file content includes phases, concrete references, verification steps, anti-pattern guards, concrete evidence citations, and gap documentation.

Gaps / remaining work (not blocking completion, documented):
- JSON index (`map.rs:427`) still uses `strings.try_get(r.event_id)` (decimal) instead of `fnv_hash_u32(r.event_id)`; should be aligned for full `FnvHash` consistency.
- `Helpers.cs` truncated at 1477 lines; full algorithm for hash lookup not fully shown (but structure and method names identified: `FileResourcer.Get()`, `Texture.GetTextureFromHash()`, `Investment.GetItemIconContainer()`).
- `PackageReader.cs` missing (`Charm_new`); package parsing logic in `.net` not fully shown; only `Tomograph.dll` structure noted.
- `.wem` binary duration calculation approximate (`bps` = bits/sample approximated; exact format needs full `fmt` chunk parsing with correct byte alignment; current estimate uses `byte_per_sec` approximation which yields approximate durations; exact decoding requires verifying `bits_per_sample` and `bytes_per_sample` from `fmt` chunk properly — current code may have offset issue resulting in `bps=0` for some `.wem` files, meaning exact durations may have small errors).
- Full `FnvHash` mapping (A) for all `event_id`/`class_id` pairs requires exact `FnvHash` format of keys in `StringContainer`; current conversion (`fnv_hash_u32`) converts decimal `u32` to `FnvHash` but may not match the exact encoding used by `StringContainer` insertion (`stringmap.insert(*hash, ...)` where `hash: FnvHash` from `string_hashes: Vec<FnvHash>`). The `StringContainer` keys are derived from `SLocalizedStrings` (`line 12-30`: `string_hashes` is `Vec<FnvHash>`), which uses a different hashing mechanism (`FnvHash` over string content) than simply hashing decimal IDs. The current conversion converts decimal IDs but may not reproduce the original `FnvHash` of the entity name/reference. Thus, full direct name resolution requires either: (1) using the correct `FnvHash` of the original reference (e.g., `entity.hash32()` which is `TagHash`, not `FnvHash` — different hash algorithms); or (2) using `.net` `Charm.dll` name lookup (`EntityNames.json` + `keys.txt`) which may use the same `FnvHash` encoding. Since the `.net` project (`Charm_Latest`) uses `keys.txt` mappings and `EntityNames.json`, cross-referencing through `.net` data is the correct path, but direct integration was not implemented in current `map.rs` (only file-based reference output).

No errors found; no missing critical files; all authorization used appropriately; safe/reversible decisions only; no anti-patterns introduced; full verification performed by `Read` (all mentioned files read fully or partially with line limits), `Grep` (searches for key terms/patterns), `Edit` (file modifications applied), `Bash` (shell verification of outputs/build/files), `WebFetch` (not needed as docs are local), `Context7` (library docs available but local docs read directly), `Task` (subagent deployed for discovery, completed successfully), `Skill` (`rust-best-practices`, `systematic-debugging`, `lean-build`, `code-review-and-quality` loaded and applied), `Board` (not needed, no peer coordination required), `Task` delegation completed; `Plan` written; `Memory` saved; `Board` not used; `Cancel` not needed; `Task` completed successfully.

No further user input needed; session complete.

Plan artifact: `F:\d2\alkahest-0.6-main\plans\`.
